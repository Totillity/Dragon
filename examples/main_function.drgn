
# The main function is automatically called by the program. ALWAYS INCLUDE ONE, and always return an int.
# The return of 0 means successful execution, just like in C.
# This is the bare minimum program.

def main() -> int {
    return 0;
}